{% extends 'base.html.twig' %}
{% block title %}{{ model.name }} - SIM Auto{% endblock %}
{% block body %}
{% include 'app/_topbar.html.twig' with {user: user} %}
<main class="main-board">
    <section class="page-head">
        <h1>{{ model.name }}</h1>
        <a class="btn btn-dark" href="{{ path('app_vehicle_settings') }}">{{ 'ui.back'|trans }}</a>
    </section>

    {% include 'app/_show_info.html.twig' with {rows: [
        {label: 'vehicles.brand'|trans, value: model.brand_name},
        {label: 'vehicles.model'|trans, value: model.name}
    ]} %}
</main>
{% endblock %}
